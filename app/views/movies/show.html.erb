<div>
	<ul class="actions">
		<li><%= link_to "Home", root_path, class: "btn btn-success"%></li>
	</ul>
</div>

<div class="panel panel-info">
	<div class="panel-heading">
		<h1><%= @movie.title %></h1>
	</div>
	<div class="panel-body">
		<p><%= @movie.description %></p>
		<p><%= @movie.year_released %></p>
	</div>
	<div class="panel-footer">
		<ul class="actions">
			<li><%= link_to "Edit Project", edit_movie_path(@movie), class: "edit" %></li>
			<li><%= link_to "Delete Project", movie_path, method: :delete, data: {confirm: "Are you sure you want to delete this project?"}, class: "delete"%></li>
		</ul>
	</div>
<div>